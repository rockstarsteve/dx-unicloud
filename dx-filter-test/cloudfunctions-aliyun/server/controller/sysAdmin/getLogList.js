const uniID = require('uni-id')
module.exports = {
	main: async (event) => {
		let res = { code : 0, msg : '调用成功' };
		// 业务逻辑开始----------------------------------------------------------- 
		res.data = '这个是登录后请求到的数据'
		// 业务逻辑结束-----------------------------------------------------------
		return res;
	}
}